{
    "name": "test",
    "version": "0.1.0",
    "description": "Cypress tests for app",
    "license": "MIT",
    "private": true,
    "scripts": {
        "cy:open": "cypress open",
        "cy:run": "cypress run",
        "cy:ci": "cy:run --no-clipboard"
    },
    "devDependencies": {
        "cypress": "^6.2.0",
        "eslint-plugin-cypress": "^2.11.2",
        "start-server-and-test": "^1.11.7"
    }
}